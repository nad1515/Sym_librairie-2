{% extends 'base.html.twig' %}

{% block title %}
 Editer une commande 
{% endblock %}

{% block body %}
<h1  class=" text-aligne text-center"> Editer une  commande </h1>

    {{ form_start(form) }}
<div class ="forme ">
    {{ form_widget(form) }}

     <button class="btn btn-primary  mt-4">{{ ' Afficher' }}</button>
   </div>
    {{ form_end(form) }}

{% endblock %}