{% extends 'base.html.twig' %}

{% block title %}
 Ajouter un  livre 
{% endblock %}

{% block body %}
<h1  class=" text-aligne text-center"> Ajouter un  livre </h1>

    {{ form_start(form) }}
<div class ="forme">
    {{ form_widget(form) }}

     <button class="btn btn-primary  mt-4">{{ ' Ajouter' }}</button>
   </div>
    {{ form_end(form) }}

{% endblock %}